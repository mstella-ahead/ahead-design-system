# Components

Reference HTML for each primitive in the system. Open these in a browser to see the live rendering.

| File | Patterns shown |
|---|---|
| `buttons.html` | All 16 button variants on light and dark surfaces |
| `cards.html` | Card variants, stat block (Pattern C) |
| `article.html` | Article hero (G), two-column body (H), pull-quote (I), tags |
| `case-study.html` | Listicle-image (J), sticky share (K), related cases (L) |

For full-page compositions, see `../examples/`.
